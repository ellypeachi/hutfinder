// Entry for the static legal pages (imprint/, privacy/). Same fonts and
// tokens as the app, no React: the pages are plain HTML.
import '@fontsource-variable/fraunces/full.css'
import '@fontsource-variable/figtree'
import './tokens.css'
import './legal.css'
