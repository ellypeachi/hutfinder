// Entry for the static pages (faq/, imprint/, privacy/). Same fonts and
// tokens as the app, no React: the pages are plain HTML.
import '@fontsource-variable/fraunces/full.css'
import '@fontsource-variable/figtree'
// The German hut words on the FAQ page are set in italic, and a browser
// would otherwise slant the roman face itself.
import '@fontsource-variable/figtree/wght-italic.css'
import './tokens.css'
import './legal.css'
